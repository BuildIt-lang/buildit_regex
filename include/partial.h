#include <iostream>
#include <set>
#include <map>
#include <vector>
#include <algorithm>

using namespace std;

vector<string> get_leftmost_longest_matches(string text, map<int, set<int>> end_to_start);
